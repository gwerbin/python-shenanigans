from setuptools import setup

setup(
    name='pattern-matching',
    author='kingdom5500',
    version='0.0.2019.07.31',  # "Alpha" versioning scheme, should probably be replaced with something else
    py_modules=['match']
)
